console.log(+true); //In JavaScript, the unary plus operator (+) can be used to convert a value to its numeric representation. When applied to a Boolean value, true is implicitly converted to 1 and false is converted to 0.Therefore, when you use + on the Boolean value true, it is converted to the numeric value 1, which is then logged to the console.

console.log(typeof +true); // type will be number
